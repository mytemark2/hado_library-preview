# Update operation

Normal updates are applied by editing source files directly and committing the completed file set together.

Preview synchronization is triggered by push to `feature/app-3.0.0.0` through `.github/workflows/notify-preview.yml`.
